# Listener Client

Frontend client workspace.

The runtime and framework are intentionally undecided for now. Generated API
types from `../proto` can live here once the client stack is chosen.

See `../docs/client-architecture.md` for the planned Flutter, Dart FFI, and Zig
audio engine split.
